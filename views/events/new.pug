extends ../layout

block content
  .container
    h1.main-title Create Event
    form(action="/events", method="POST")
      include _form
      button.btn.btn-outline-primary(type="이벤트 생성") Create Your Event