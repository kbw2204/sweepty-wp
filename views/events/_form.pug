
.form-group
  label(for="title") Title
  input.form-control(type="text", name="title", placeholder="Name of Event", value=event.title)
.form-group
  label(for="group_name") Group Name
  input.form-control(type="text", name="group_name", placeholder="Name of Group", value=event.group_name)
.form-group
  label(for="about_group") About Group
  textarea.form-control(name="about_group", rows="2", value= event.about_group)
.container
  .row
    .col-6
      .form-group
        label(for="eventtype") Event Type
        select.form-control(name="eventtype", value = event.eventtype)
          option 교육/강연
          option 전시/박람회
          option 세미나/컨퍼런스
          option 문화/예술
          option 소모임
          option 기타
    .col-6
      .form-group
        label(for="eventtopic") Event Topic
        select.form-control(name="eventtopic", value=event.eventtopic)
          option 과학/기술
          option 취미
          option 음악
          option 미술
          option 라이프스타일
          option 비즈니스
          option 스포츠
          option 여행
          option 기타	
//- .form-group
//-   label(for='img') Image
//-   input.form-control(type="file",name="img")

.form-group
  label(for="total_p_num") 참가 가능 인원
  input(type='number', name='total_p_num', min='1', max='99999',value=event.total_p_num)
.form-group
  label(for="place") 장소
  input.form-control(type="text", name="place", placeholder="") 
.form-group
  label(for="daterange") 시작 시간 ~ 종료 시간 선택
  input(type='text', name='daterange',value=event.daterange)
  script(type='text/javascript').
    $(function() {
      $('input[name="daterange"]').daterangepicker({
        timePicker: true,
        timePickerIncrement: 30,
        locale: {
        format: 'YYYY/MM/DD h:mm A'
      }
    });
    });
.container
  .row
    .col-12
      label(for="cost") 이벤트 가격
    .col-6
      .form-check
        label.form-check-label
        input#costFree.form-check-input(type="radio" name="exampleRadios" checked="")
        |
        | FREE

      

    .col-6
      .form-check
        .input-group
          span.input-group-addon
            input#cost(type="radio" name="exampleRadios")
            input(type='number', name='cost', min='0', max='999999')
            span.input-group-addon 원

.form-group
  label(for="content") Contents
  textarea.form-control(name="content", rows="10")

.form-group
  label(for="tags") Tags
  input.form-control(type="text", name="tags", placeholder="tags separated by spaces", value=event.tags ? event.tags.join(" ") : "")

